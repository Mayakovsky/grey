# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# turbo
.turbo/

# test coverage
coverage/

# env — NEVER commit real secrets. .env.example IS committed.
.env
.env.local
.env.*.local

# M4 Phase B — Base Sepolia smoke fixture: throwaway testnet keystores +
# extracted keys. Worthless-by-design but NEVER committed (keys stay off git).
.sepolia/

# logs
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# NOTE: pnpm-lock.yaml is intentionally NOT ignored — it is committed.
