# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# turbo
.turbo/

# test coverage
coverage/

# env — NEVER commit real secrets. .env.example IS committed.
.env
.env.local
.env.*.local

# M4 Phase B — Base Sepolia smoke fixture: throwaway testnet keystores +
# extracted keys. Worthless-by-design but NEVER committed (keys stay off git).
.sepolia/

# CDP indexing mainnet test — single-purpose real-funds keystore. NEVER committed.
.cdp-mainnet-test/

# CDP organic-settlement — single-purpose real-funds keystore for the organic-production
# settlement + crawler-check directive. NEVER committed.
.cdp-organic-settlement/

# one-off local review-diff artifacts (pre-merge review copies of branches that landed on main;
# redundant with git history once merged — verified per-file before adding this, 2026-08-07).
review-*.diff

# one-off ops script: Cluster A (postgresql-x64-16) stop+disable, run by Forces and independently
# verified complete (BION-POSTGRES-SERVICE-REGISTRATION-REPORT-KOV.md) — situation resolved.
stop-disable-cluster-a.ps1

# logs
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# NOTE: pnpm-lock.yaml is intentionally NOT ignored — it is committed.
