/* eslint-disable */
/**
 * AUTO-GENERATED by `pnpm codegen` from src/responses/v1/*.schema.json.
 * DO NOT EDIT BY HAND. Re-run codegen to regenerate.
 */

/**
 * Common response envelope for all Grey v1 offerings. Exactly one of `payload` / `error` is present. `payload` is discriminated on `offering` via allOf[if/then] (JSON Schema 2020-12 has no native discriminator keyword; a bare oneOf of the response $refs is ambiguous because the tier schemas are nested supersets).
 */
export type GreyResponseEnvelope = {
  [k: string]: unknown;
} & {
  /**
   * Envelope contract version. Literal 'v1' for the M2.5 layer.
   */
  schemaVersion: "v1";
  /**
   * Canonical offering slug. Discriminates the payload shape.
   */
  offering:
    | "legitimacy_scan"
    | "verify_whitepaper"
    | "verify_full_tech"
    | "claim_extraction"
    | "claim_history"
    | "quick_protocol_facts"
    | "daily_tech_brief"
    | "daily_greenlight_list"
    | "scam_alert_feed";
  /**
   * Correlation id (UUID).
   */
  requestId: string;
  agent: {
    /**
     * Agent DID. Placeholder-tolerant in M2.5 (real DIDs minted in M4); accepts any did:* namespace (e.g. did:erc8004:..., did:placeholder:grey).
     */
    did: string;
    name: string;
    runtime: string;
  };
  subject: {
    tokenAddress: string | null;
    projectName: string;
  };
  /**
   * Offering response payload. Shape is bound to `offering` by the allOf[if/then] clauses below.
   */
  payload?: {};
  /**
   * Present instead of `payload` on a true failure. Cache-miss verdicts (NOT_IN_DATABASE, INSUFFICIENT_DATA) are valid payloads, not errors.
   */
  error?: {
    code:
      | "BAD_REQUEST"
      | "PAYMENT_REQUIRED"
      | "PAYMENT_INSUFFICIENT"
      | "RATE_LIMITED"
      | "PIPELINE_ERROR"
      | "UPSTREAM_TIMEOUT"
      | "INTERNAL_ERROR";
    message: string;
    retryable: boolean;
  };
  metadata: {
    costUsd: number;
    model: string;
    latencyMs: number;
    /**
     * ISO 8601 UTC.
     */
    timestamp: string;
    cacheHit: boolean;
  };
} & (
    | {
        payload: unknown;
        error?: never;
      }
    | {
        error: unknown;
        payload?: never;
      }
  );
