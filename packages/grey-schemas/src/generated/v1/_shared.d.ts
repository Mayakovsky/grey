/* eslint-disable */
/**
 * AUTO-GENERATED by `pnpm codegen` from src/responses/v1/*.schema.json.
 * DO NOT EDIT BY HAND. Re-run codegen to regenerate.
 */

/**
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "Verdict".
 */
export type Verdict = "PASS" | "CONDITIONAL" | "FAIL" | "INSUFFICIENT_DATA" | "NOT_IN_DATABASE";
/**
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "ClaimCategory".
 */
export type ClaimCategory = "TOKENOMICS" | "PERFORMANCE" | "CONSENSUS" | "SCIENTIFIC";
/**
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "WhitepaperStatus".
 */
export type WhitepaperStatus = "DISCOVERED" | "INGESTED" | "VERIFYING" | "VERIFIED" | "FAILED";
/**
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "MathValidity".
 */
export type MathValidity = "VALID" | "FLAWED" | "UNVERIFIABLE";
/**
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "Plausibility".
 */
export type Plausibility = "HIGH" | "LOW" | "OUTLIER";
/**
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "Originality".
 */
export type Originality = "NOVEL" | "DERIVATIVE" | "PLAGIARIZED";
/**
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "Consistency".
 */
export type Consistency = "CONSISTENT" | "CONTRADICTED";
/**
 * Union type alias (TS-side); whether a whitepaper claims MiCA compliance.
 *
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "MicaClaimStatus".
 */
export type MicaClaimStatus = "YES" | "NO" | "NOT_MENTIONED";
/**
 * Union type alias (TS-side); assessed MiCA compliance state.
 *
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "MicaComplianceStatus".
 */
export type MicaComplianceStatus = "YES" | "NO" | "PARTIAL" | "NOT_APPLICABLE";
/**
 * Union type alias (TS-side); tier selection outcome for the whitepaper source.
 *
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "DiscoveryStatus".
 */
export type DiscoveryStatus = "cached" | "provided" | "primary" | "community" | "aggregator" | "failed";

/**
 * Shared $defs for the Grey v1 response schemas. Enums + reused interfaces, derived from @grey/schemas/src/index.ts (M2 domain types). 7 real TS enums + 3 union type aliases + 6 reused interfaces. The enum-vs-union distinction is TS-side only (the enum-drift test mechanism differs); at the JSON Schema level all closed value sets use `enum` arrays.
 */
export interface GreySharedDefs {
  [k: string]: unknown;
}
/**
 * A single testable claim extracted from a whitepaper (L2 output).
 *
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "ExtractedClaim".
 */
export interface ExtractedClaim {
  claimId: string;
  category: ClaimCategory;
  claimText: string;
  statedEvidence: string;
  mathematicalProofPresent: boolean;
  sourceSection: string;
  /**
   * True if the claim relates to regulatory compliance (MiCA, KYC/AML, ESMA, etc.).
   */
  regulatoryRelevance: boolean;
}
/**
 * Per-claim evaluation result (L3 output). All scoring fields optional; absent means that method did not run for the claim.
 *
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "ClaimEvaluation".
 */
export interface ClaimEvaluation {
  claimId: string;
  mathValidity?: MathValidity;
  benchmarkDelta?: number;
  plausibility?: Plausibility;
  citationSupportsClaim?: boolean | null;
  originality?: Originality;
  consistency?: Consistency;
}
/**
 * Per-tier record for the discoveryAttempts array in deliverables.
 *
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "DiscoveryAttempt".
 */
export interface DiscoveryAttempt {
  /**
   * 0..4
   */
  tier: number;
  /**
   * DiscoveryStatus | 'skipped' | 'error'.
   */
  status: DiscoveryStatus | ("skipped" | "error");
  structuralScore?: number;
  claimCount?: number;
  note?: string;
}
/**
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "MicaAnalysis".
 */
export interface MicaAnalysis {
  claimsMicaCompliance: MicaClaimStatus;
  micaCompliant: MicaComplianceStatus;
  micaSummary: string;
  /**
   * Which of the 7 required MiCA sections were found.
   */
  micaSectionsFound: string[];
  /**
   * Which of the 7 required MiCA sections are missing.
   */
  micaSectionsMissing: string[];
}
/**
 * L1 structural analysis output.
 *
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "StructuralAnalysis".
 */
export interface StructuralAnalysis {
  hasAbstract: boolean;
  hasMethodology: boolean;
  hasTokenomics: boolean;
  hasReferences: boolean;
  citationCount: number;
  verifiedCitationRatio: number;
  hasMath: boolean;
  mathDensityScore: number;
  coherenceScore: number;
  similarityTopMatch: string | null;
  similarityScore: number;
  hasAuthors: boolean;
  hasDates: boolean;
  mica: MicaAnalysis;
}
/**
 * Canonical whitepaper record. ingestedAt is a Date in the pipeline; at the response boundary it serializes to an ISO 8601 date-time string (Q7).
 *
 * This interface was referenced by `GreySharedDefs`'s JSON-Schema
 * via the `definition` "WhitepaperRecord".
 */
export interface WhitepaperRecord {
  id: string;
  projectName: string;
  tokenAddress: string | null;
  chain: string;
  documentUrl: string;
  ipfsCid: string | null;
  knowledgeItemId: string | null;
  pageCount: number;
  ingestedAt: string;
  status: WhitepaperStatus;
  selectionScore: number;
  metadataJson: {};
}
