# @grey/x402-middleware

x402 adapter wiring grey-core's offerings as x402-paid endpoints (receiver only —
never signs; the sweeper holds the only key).

Filled in **Step 7** of Phase 2. See `phase2-work-breakdown-kovsky.md` and
`x402-middleware-adapter-skeleton.md`.
