-- NOT YET APPLIED to Supabase. Application is a Step 2 task under separate authorization.
-- Step 1 creates the schema only; table DDL (whitepapers, verifications, claims,
-- embeddings, cost_events, requests, sweep_log) lands in Step 2.
CREATE SCHEMA IF NOT EXISTS grey_two;
