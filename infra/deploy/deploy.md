# Deployment

Populated in Step 8 (Movement 5). Do not deploy from this scaffold.

grey-core and grey-sweeper deploy as two independent systemd units on the VPS,
alongside (never coupled to) ElizaOS Grey. No auto-deploy from CI.
